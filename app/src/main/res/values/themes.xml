<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="TalesTheme"
        parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/talesActionBar</item>
        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/talesActionBar</item>
        <item name="android:windowBackground">@drawable/bg_paper</item>
    </style>

    <!-- ActionBar styles -->
    <style name="talesActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@drawable/actionbar_bg</item>
        <item name="android:homeAsUpIndicator">@mipmap/btn_back</item>

        <!-- Support library compatibility -->
        <item name="background">@drawable/actionbar_bg</item>
        <item name="homeAsUpIndicator">@mipmap/btn_back</item>
    </style>

</resources>